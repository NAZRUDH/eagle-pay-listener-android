<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Night (Dark) theme -->
    <style name="Theme.EaglePayListener" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/eagle_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/eagle_primary_dark</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/bg_dark</item>
        <item name="android:windowBackground">@color/bg_dark</item>
    </style>
</resources>
